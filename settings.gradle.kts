pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "ShrinkGuard"

include(":shrinkguard-core")
include(":shrinkguard-gradle-plugin")
